import React, { useState } from 'react';

export default () => {
  console.log(SimpleMDE);

  return (
    <div>
      <h1 className={styles.title}>Page test</h1>
      <div>{text}</div>
    </div>
  );
};
